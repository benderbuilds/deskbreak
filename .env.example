# Stripe (optional — leave blank to use the paywall + demo unlock)
STRIPE_SECRET_KEY=
NEXT_PUBLIC_STRIPE_PRICE_ID=

# Set to true to show “Unlock Pro for demo” even in production builds
NEXT_PUBLIC_DEMO_UNLOCK=true

# Optional absolute origin for Checkout redirect URLs
NEXT_PUBLIC_APP_URL=http://localhost:3000
